% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/association.R
\name{ds.flower.associate}
\alias{ds.flower.associate}
\title{Differentially-private pooled binary association}
\usage{
ds.flower.associate(
  conns,
  outcome,
  exposure,
  outcome_levels,
  exposure_levels,
  data = NULL,
  resource = NULL,
  symbol = NULL,
  verbose = FALSE,
  silent = FALSE,
  allow_insecure_http = getOption("dsflower.dsi_allow_insecure_http", character()),
  resource_kind = "imaging"
)
}
\arguments{
\item{conns}{DSI connections.}

\item{outcome}{One outcome column name.}

\item{exposure}{One exposure column name.}

\item{outcome_levels}{Ordered public \code{c(reference, positive)} levels.}

\item{exposure_levels}{Ordered public \code{c(reference, positive)} levels.}

\item{data}{Optional server-side data source.}

\item{resource}{Optional Opal resource name.}

\item{symbol}{Optional assigned server-side symbol.}

\item{verbose}{Show Flower output.}

\item{silent}{Suppress progress messages.}

\item{allow_insecure_http}{Exact connection names allowed to use HTTP.}

\item{resource_kind}{Explicit Opal resource route, exactly
\code{"imaging"} or \code{"tabular"}.}
}
\value{
A \code{dsflower_association}. If a complete release is unavailable,
  \code{available} is false and table/measures/noise are null.
}
\description{
Builds one bounded 3x3 exposure/outcome table at each node, applies one
server-owned sticky Gaussian release, and returns only the pooled table and
descriptive prevalence measures. Values other than the two ordered public
levels are retained in the \code{unknown} row or column. No exact or per-node
counts leave a data node.
}
\details{
This is a descriptive association, not a causal or adjusted effect. Under
patient privacy, each axis means "ever positive" across all rows for that
patient; exposure and outcome need not occur at the same visit.
}
