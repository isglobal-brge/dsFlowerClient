% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recipe.R
\name{ds.flower.recipe}
\alias{ds.flower.recipe}
\title{Create a Flower federated learning recipe}
\usage{
ds.flower.recipe(
  model,
  strategy = ds.flower.strategy.fedavg(),
  task = NULL,
  num_rounds = 5L,
  target = NULL,
  features = NULL
)
}
\arguments{
\item{model}{A \code{dsflower_model} object or character model name
accepted by \code{ds.flower.model()}.}

\item{strategy}{A \code{dsflower_strategy} object or character strategy
name accepted by \code{ds.flower.strategy()}.}

\item{task}{A \code{dsflower_task} object, character task name, or NULL to
infer from model.}

\item{num_rounds}{Integer; number of federated training rounds.}

\item{target}{Character; target column name(s). Multiple targets are supported
only by the multilabel enforced-DP model.}

\item{features}{Character vector; feature column names, or NULL for auto.}
}
\value{
A \code{dsflower_recipe} S3 object.
}
\description{
A recipe combines the analyst-controlled specification objects needed for a
federated learning experiment. Privacy policy is not part of the recipe; it
is selected and enforced by each data node. Task can be inferred from the
model when not specified.
}
