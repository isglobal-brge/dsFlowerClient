% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_submit.R
\name{.compute_feature_norm}
\alias{.compute_feature_norm}
\title{Compute GLOBAL per-feature mean/SD across nodes for DP-SGD standardization}
\usage{
.compute_feature_norm(conns, symbol, features)
}
\arguments{
\item{conns}{DSI connections.}

\item{symbol}{Character; the assigned data symbol (e.g. "D").}

\item{features}{Character; feature column names (defines output order).}
}
\value{
list(means, sds) aligned to \code{features}, or NULL.
}
\description{
Calls the disclosure-controlled \code{flowerFeatureStatsDS} aggregate on every
node, sums the per-feature sufficient statistics (count / sum / sum-of-squares)
BY FEATURE NAME (so a node returning a column subset or different order still
aligns), then derives the pooled population mean and SD. Returns means/SDs in
the exact \code{features} order, fully validated (finite; constant/degenerate
features get SD 1 -> a no-op scaling). Returns NULL if no node responds.
}
\keyword{internal}
