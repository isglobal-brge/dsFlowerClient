% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convenience.R
\name{ds.flower.fit}
\alias{ds.flower.fit}
\title{Fit a federated model in one call}
\usage{
ds.flower.fit(
  conns,
  data = NULL,
  resource = NULL,
  symbol = NULL,
  target,
  features = NULL,
  model = "pytorch_logreg",
  model_params = list(),
  torch_backend = "auto",
  strategy = "fedavg",
  strategy_params = list(),
  rounds = 5L,
  task = NULL,
  output_dir = NULL,
  output_name = NULL,
  silent = FALSE,
  verbose = FALSE,
  feature_bounds = NULL,
  feature_cuts = NULL,
  target_levels = NULL,
  target_bounds = NULL,
  allow_insecure_http = getOption("dsflower.dsi_allow_insecure_http", character()),
  data_kind = NULL,
  holdout = NULL
)
}
\arguments{
\item{conns}{DSI connections object.}

\item{data}{Optional character data source resolved by \code{ds.flower.connect()}.}

\item{resource}{Optional Opal resource name.}

\item{symbol}{Optional assigned DataSHIELD symbol. Defaults to \code{"D"}
when \code{data}, \code{resource}, and \code{symbol} are all NULL.}

\item{target}{One target-column name, or exactly \code{num_labels} distinct
target-column names for \code{pytorch_multilabel}.}

\item{features}{Character vector of feature column names, or NULL for
model-specific auto handling.}

\item{model}{Character model name or \code{dsflower_model} object.}

\item{model_params}{Named list passed to \code{ds.flower.model()} when
\code{model} is a character value.}

\item{torch_backend}{Character; requested node-side torch backend
(\code{"auto"}, \code{"cpu"}, or a GPU selector).}

\item{strategy}{Character strategy name or \code{dsflower_strategy} object.}

\item{strategy_params}{Named list passed to \code{ds.flower.strategy()} when
\code{strategy} is a character value.}

\item{rounds}{Integer number of federated rounds.}

\item{task}{Optional character task name or \code{dsflower_task} object.}

\item{output_dir}{Optional character; parent directory the trained model is
saved under (created if missing). Defaults to \code{./dsflower_output}.}

\item{output_name}{Optional character; folder/file name for this model inside
\code{output_dir} (extension is added automatically). Defaults to an
auto-generated model id.}

\item{silent}{Logical; when \code{TRUE}, suppress the training progress
feedback (connection, per-round and completion messages).}

\item{verbose}{Logical; when \code{TRUE}, also print the raw flwr run log
(debugging). The tidy per-round progress is shown regardless unless
\code{silent = TRUE}.}

\item{feature_bounds}{Optional public feature bounds as
\code{list(lower=..., upper=...)} in feature order.}

\item{feature_cuts}{Required for native-tight tree models: a list containing
one strictly increasing vector of public cut points per feature, each
strictly inside \code{feature_bounds}. For XGBoost, seven data-independent
cuts per feature are the benchmark-backed starting point; they are never
inferred from private node data.}

\item{target_levels}{Optional ordered public classification label vocabulary.
Non-numeric labels require it; missing or unknown values map to public code
zero. Multilabel applies the same public two-level vocabulary independently
to every target column. Binary XGBoost requires exactly two ordered levels.}

\item{target_bounds}{Required public \code{list(lower=..., upper=...)} for
regression/count models.}

\item{allow_insecure_http}{Character vector of exact connection names allowed
to use plaintext HTTP. Empty by default. This exception does not provide
transport security; use it only behind an independently trusted network.}

\item{data_kind}{Optional input kind, \code{"tabular"} or \code{"image"}.
It is inferred when the registered model supports exactly one kind; models
registered for both require an explicit choice.}

\item{holdout}{Optional numeric test fraction strictly between zero and one,
with at most six decimal places. The node assigns complete privacy units
before training, trains only on the complement, and returns the final model
together with one pooled differentially-private test metric release.}
}
\value{
A \code{dsflower_run} object.
}
\description{
High-level convenience API for the common workflow: connect to assigned
DataSHIELD data, build a recipe, run Flower, clean up server-side handles,
and return the trained run object. Advanced users can call
\code{ds.flower.submit()} directly for finer control.
}
