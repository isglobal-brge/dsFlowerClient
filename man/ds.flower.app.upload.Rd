% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/app_upload.R
\name{ds.flower.app.upload}
\alias{ds.flower.app.upload}
\title{Upload a Flower app to the nodes and verify it by sha256 (Tier-2)}
\usage{
ds.flower.app.upload(conns, app_dir, chunk_bytes = 262144L, verbose = TRUE)
}
\arguments{
\item{conns}{DSI connections object.}

\item{app_dir}{Character; path to the Flower app directory to bundle.}

\item{chunk_bytes}{Integer; bytes per push (default 256 KiB).}

\item{verbose}{Logical; print progress (default TRUE).}
}
\value{
A \code{dsflower_app} object: list(token, sha256, size).
}
\description{
Builds \code{app_dir} into a FAB, pushes it to every node in \code{conns} in
idempotent base64 chunks over DataSHIELD, then installs it (the node verifies
the sha256 and rejects any mismatch). Returns a handle carrying the upload
token + validated hash; the run then executes only this hash-pinned app.
}
