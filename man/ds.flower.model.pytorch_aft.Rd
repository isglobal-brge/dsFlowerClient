% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{ds.flower.model.pytorch_aft}
\alias{ds.flower.model.pytorch_aft}
\title{Create a fixed-dispersion accelerated failure time model}
\usage{
ds.flower.model.pytorch_aft(
  horizon,
  distribution = "weibull",
  dispersion = 1,
  time_scale = 1,
  t_min = 1,
  time_unit = "days",
  time_origin = "baseline",
  hidden_layers = integer(0),
  ...
)
}
\arguments{
\item{horizon}{Public administrative censoring horizon.}

\item{distribution}{Either `"weibull"` or `"lognormal"`.}

\item{dispersion}{Fixed public Weibull shape or log-normal sigma, one of
`c(0.5, 1, 2)`.}

\item{time_scale}{Positive public time scale (AFT density includes its Jacobian).}

\item{t_min}{Positive public minimum time resolution.}

\item{time_unit, time_origin}{Public labels for the outcome time convention.}

\item{hidden_layers}{Integer vector of hidden widths; empty means linear.}

\item{...}{Additional public neural parameters accepted by `ds.flower.model()`.}
}
\value{
A `dsflower_model` specification.
}
\description{
Each privacy unit is one subject under the custodian's patient policy.
Supply ordered targets `c(time_column, event_column)`, where event is 1 and
right censoring is 0. The public horizon must be chosen before inspecting
private outcomes. Location is bounded to [-10, 10]. Private validation,
holdout and cross-validation release bounded likelihood and observed-status
Brier metrics. Pairwise concordance remains a public-split metric.
}
