% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coordinator.R
\name{ds.flower.coordinator.up}
\alias{ds.flower.coordinator.up}
\title{Stand up a public coordinator (SuperLink)}
\usage{
ds.flower.coordinator.up(
  public_dns,
  fleet_port = 9092L,
  control_port = 9093L,
  serverappio_port = 9091L,
  cert_dir = NULL,
  cert_days = 90L,
  authorized_keys = NULL
)
}
\arguments{
\item{public_dns}{Character; the public DNS name (or IP) nodes dial, e.g.
"coordinator.datashield.live". Becomes the certificate SAN.}

\item{fleet_port}{Integer; Fleet API port SuperNodes dial (default 9092).}

\item{control_port}{Integer; Control API port \code{flwr run} dials
(default 9093).}

\item{serverappio_port}{Integer; ServerAppIO API port (default 9091).}

\item{cert_dir}{Character or NULL; directory for certs + logs. Defaults to a
stable dir under the client data root.}

\item{cert_days}{Integer; certificate validity in days (default 90).}
}
\value{
Invisible list: superlink_address, control_address, ca_cert_path,
  ca_cert_pem, ca_cert_b64, pid, log_path, process.
}
\description{
Launches a long-lived \code{flower-superlink} with a TLS server certificate
whose SAN matches \code{public_dns}, so remote SuperNodes (and the
researcher's \code{flwr run}) can dial it by name across NAT. Prints the
exact values to configure on each node (\code{dsflower.coordinator_*}); after
that the researcher just calls \code{ds.flower.fit()} and the address/CA are
auto-discovered.
}
\details{
Run this ONCE on a host that every node can open an outbound connection to
(a small public VM, or one of the federation's public hosts).
}
