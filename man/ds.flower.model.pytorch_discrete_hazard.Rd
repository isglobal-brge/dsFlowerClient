% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survival.R
\name{ds.flower.model.pytorch_discrete_hazard}
\alias{ds.flower.model.pytorch_discrete_hazard}
\title{Create a subject-level discrete hazard model}
\usage{
ds.flower.model.pytorch_discrete_hazard(
  edges,
  t_min = 1,
  time_unit = "days",
  time_origin = "baseline",
  hidden_layers = integer(0),
  ...
)
}
\arguments{
\item{edges}{Strictly increasing public interval endpoints starting at zero;
length between 2 and 65 (1 <= K <= 64). The final edge is the horizon.}

\item{t_min}{Positive public minimum time resolution.}

\item{time_unit, time_origin}{Public labels; schema v1 uses days/from-baseline.}

\item{hidden_layers}{Integer vector of hidden widths; empty means linear.}

\item{...}{Additional public neural parameters accepted by `ds.flower.model()`.}
}
\value{
A `dsflower_model` specification.
}
\description{
Interval j is `(edges[j], edges[j+1]]`. Events contribute through the interval
containing their time; censoring contributes only fully observed intervals.
An event on an interval endpoint belongs to that interval. Source times above
the public horizon are administratively censored at the horizon. The masked
binary log likelihood is summed per subject and divided by public K; each
subject is sampled and clipped once. Invalid subjects remain in the privacy
census and have a zero loss contribution.
}
