% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlay.R
\name{ds.flower.overlay.up}
\alias{ds.flower.overlay.up}
\title{Join the data nodes to the tailnet (overlay transport)}
\usage{
ds.flower.overlay.up(
  conns,
  authkey,
  symbol = "flower",
  hostname_prefix = "dsflower-node"
)
}
\arguments{
\item{conns}{DSI connections object.}

\item{authkey}{Character; a reusable Tailscale auth key (tskey-...). Pushed to
the nodes over DataSHIELD; prefer an ephemeral, tagged key.}

\item{symbol}{Character; handle symbol name (default "flower").}

\item{hostname_prefix}{Character; tailnet hostname prefix per node.}
}
\value{
A \code{dsflower_result} with per-node join status.
}
\description{
Brings each node onto your Tailscale tailnet (userspace mode, no root) so its
SuperNode can reach the SuperLink on this machine across NAT. Requires that
THIS machine is already on the tailnet (install Tailscale + `tailscale up`).
}
