% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{ds.flower.model.lightgbm}
\alias{ds.flower.model.lightgbm}
\title{Create a dsFlower LightGBM-style private boosting request}
\usage{
ds.flower.model.lightgbm(
  task = c("binary", "regression"),
  n_estimators = 8L,
  max_depth = 2L,
  num_leaves = 4L,
  learning_rate = 0.25,
  min_data_in_leaf = 1L,
  min_gain_to_split = 0,
  reg_alpha = 0,
  reg_lambda = 1,
  max_delta_step = 1
)
}
\arguments{
\item{task}{Either \code{"binary"} or \code{"regression"}.}

\item{n_estimators}{Positive boosting iteration count.}

\item{max_depth}{Positive maximum depth, at most 32.}

\item{num_leaves}{Positive leaf ceiling between 2 and 256 and compatible with
\code{max_depth}.}

\item{learning_rate}{Numeric in \code{(0,1]}.}

\item{min_data_in_leaf}{Positive public minimum noisy count used for splits.}

\item{min_gain_to_split}{Non-negative public split threshold.}

\item{reg_alpha}{Non-negative L1 leaf regularization.}

\item{reg_lambda}{Positive L2 leaf regularization.}

\item{max_delta_step}{Positive public leaf-step bound.}
}
\value{
A \code{dsflower_model} request object.
}
\description{
This is dsFlower's reviewed asymmetric public-bin numeric boosting engine. It
does not load or claim compatibility with the upstream LightGBM binary/model
runtime. Splits follow a fixed public transcript and private histograms are
released only through the node-owned sticky Gaussian mechanism.
}
