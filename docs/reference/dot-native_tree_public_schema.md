# Canonicalise and hash the public feature and target schema

Canonicalise and hash the public feature and target schema

## Usage

``` r
.native_tree_public_schema(features, bounds, cuts, target, task, schema_sha256)
```
