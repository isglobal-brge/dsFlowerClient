# Validate dsFlower boosting-style parameters before the public schema exists

Validate dsFlower boosting-style parameters before the public schema
exists

## Usage

``` r
.validate_boosting_style_model_params(params, engine)
```
