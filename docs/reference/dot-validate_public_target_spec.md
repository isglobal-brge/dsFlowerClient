# Validate data-independent target semantics

Validate data-independent target semantics

## Usage

``` r
.validate_public_target_spec(
  levels,
  bounds,
  task_type,
  loss_name = "",
  n_classes = 2L
)
```
