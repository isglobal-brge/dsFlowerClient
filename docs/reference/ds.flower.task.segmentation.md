# Create a segmentation task specification

For image segmentation models (e.g. U-Net). Labels are mask images.

## Usage

``` r
ds.flower.task.segmentation()
```

## Value

A `dsflower_task` S3 object with type = "segmentation".
