# Read saved model weights from results directory

Read saved model weights from results directory

## Usage

``` r
.read_model_weights(results_dir)
```

## Arguments

- results_dir:

  Character; path to the results directory.

## Value

A list of numeric arrays (one per parameter), or NULL.
