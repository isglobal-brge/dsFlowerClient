# Stream one archive to every node with exact offset acknowledgements

Stream one archive to every node with exact offset acknowledgements

## Usage

``` r
.push_app_archive(conns, path, token, chunk_bytes, progress = NULL)
```
