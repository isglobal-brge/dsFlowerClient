# Build the submission FAB: the trusted runner skeleton + (neural/egress) the user package + a pyproject carrying the dp-track and run config.

Build the submission FAB: the trusted runner skeleton + (neural/egress)
the user package + a pyproject carrying the dp-track and run config.

## Usage

``` r
.build_submission_app(sub, config_lines, results_dir, vision = FALSE)
```
