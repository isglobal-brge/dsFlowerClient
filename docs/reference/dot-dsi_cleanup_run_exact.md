# Exact per-node rollback of partial multi-node run preparation

Exact per-node rollback of partial multi-node run preparation

## Usage

``` r
.dsi_cleanup_run_exact(conns, symbol, attempts = 3L)
```
