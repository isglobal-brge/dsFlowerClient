# Build and pin a native-tree manifest without exposing a public model yet

Build and pin a native-tree manifest without exposing a public model yet

## Usage

``` r
.build_native_tree_manifest(
  engine,
  mode,
  task,
  features,
  bounds,
  target,
  cuts = NULL,
  parameters = list(),
  resources = list(),
  schema_sha256 = NULL
)
```
