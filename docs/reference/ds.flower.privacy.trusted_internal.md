# Create a trusted_internal privacy spec

For trusted internal collaborations. Per-node metrics allowed, no
SecAgg.

## Usage

``` r
ds.flower.privacy.trusted_internal()
```

## Value

A `dsflower_privacy` S3 object with mode = "trusted_internal".
