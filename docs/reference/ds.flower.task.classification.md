# Create a classification task specification

Create a classification task specification

## Usage

``` r
ds.flower.task.classification()
```

## Value

A `dsflower_task` S3 object with type = "classification".
