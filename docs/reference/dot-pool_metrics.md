# Pool metrics across servers

Pool metrics across servers

## Usage

``` r
.pool_metrics(results)
```

## Arguments

- results:

  Named list of per-server metric data.frames.

## Value

A pooled data.frame with mean values.
