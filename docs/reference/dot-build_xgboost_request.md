# Build one canonical analyst-facing XGBoost request

Build one canonical analyst-facing XGBoost request

## Usage

``` r
.build_xgboost_request(
  params,
  features,
  feature_bounds,
  feature_cuts,
  target_name,
  target_levels = NULL,
  target_bounds = NULL,
  schema_sha256 = NULL
)
```
