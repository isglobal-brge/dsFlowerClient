# Canonicalise public HookApp parameters for the cross-runtime contract

Canonicalise public HookApp parameters for the cross-runtime contract

## Usage

``` r
.canonical_hook_app_params(app_params = list())
```
