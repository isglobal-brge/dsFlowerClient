# Whether a parameter name could alter privacy or execute external code

Whether a parameter name could alter privacy or execute external code

## Usage

``` r
.native_tree_reserved_parameter(name)
```
