# Read the training feature names (in order) from a model dir's metadata.json

Read the training feature names (in order) from a model dir's
metadata.json

## Usage

``` r
.read_meta_features(model_dir)
```
