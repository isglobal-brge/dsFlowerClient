# Stop the Flower SuperLink

Sends SIGTERM, waits, then SIGKILL if needed. Works for both interactive
and detached SuperLinks.

## Usage

``` r
ds.flower.superlink.stop()
```

## Value

Invisible TRUE.
