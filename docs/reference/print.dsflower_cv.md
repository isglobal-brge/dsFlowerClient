# Print a federated cross-validation result

Print a federated cross-validation result

## Usage

``` r
# S3 method for class 'dsflower_cv'
print(x, ...)
```

## Arguments

- x:

  A `dsflower_cv` object.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns `x`.
