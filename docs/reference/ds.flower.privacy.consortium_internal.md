# Create a consortium_internal privacy spec

For consortium collaborations. Per-node metrics suppressed, fixed
sampling.

## Usage

``` r
ds.flower.privacy.consortium_internal()
```

## Value

A `dsflower_privacy` S3 object with mode = "consortium_internal".
