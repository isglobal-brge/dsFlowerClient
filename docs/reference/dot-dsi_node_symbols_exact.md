# Read one node's symbol table without accepting a misassociated response

Read one node's symbol table without accepting a misassociated response

## Usage

``` r
.dsi_node_symbols_exact(conns, host)
```
