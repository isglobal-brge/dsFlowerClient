# Resolve model info for prediction

Finds the native model file and determines the framework.

## Usage

``` r
.resolve_model_for_predict(model)
```

## Arguments

- model:

  dsflower_run, list, or path.

## Value

List with model_file and framework.
