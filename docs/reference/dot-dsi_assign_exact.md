# Require an explicit successful DSI ASSIGN callback from every node

Require an explicit successful DSI ASSIGN callback from every node

## Usage

``` r
.dsi_assign_exact(conns, operation, invoke)
```
