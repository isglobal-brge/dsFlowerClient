# Pip dependencies for the trusted app (one app, not per-model). Vision adds torchvision + the medical-image readers (2D, plus the MONAI 3D path).

Pip dependencies for the trusted app (one app, not per-model). Vision
adds torchvision + the medical-image readers (2D, plus the MONAI 3D
path).

## Usage

``` r
.harness_dependencies(vision = FALSE)
```
