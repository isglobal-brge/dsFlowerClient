# Build one canonical analyst-facing ExtraTrees request

Build one canonical analyst-facing ExtraTrees request

## Usage

``` r
.build_extra_trees_request(
  params,
  features,
  feature_bounds,
  feature_cuts,
  target_name,
  target_levels = NULL,
  target_bounds = NULL,
  schema_sha256 = NULL
)
```
