# Delete a saved model

Removes a model output directory and all its contents.

## Usage

``` r
ds.flower.delete_model(path, confirm = TRUE)
```

## Arguments

- path:

  Character; path to the model directory.

- confirm:

  Logical; if TRUE (default), ask for confirmation.

## Value

Invisible TRUE.
