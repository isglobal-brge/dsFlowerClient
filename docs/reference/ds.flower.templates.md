# List templates available on the servers

Queries each server for its installed templates.

## Usage

``` r
ds.flower.templates(conns)
```

## Arguments

- conns:

  DSI connections object.

## Value

A named list mapping server names to character vectors of template
names.
