# Decode and re-pin exact canonical native-tree request bytes

Decode and re-pin exact canonical native-tree request bytes

## Usage

``` r
.validate_native_tree_request_wire(request_b64, request_sha256)
```
