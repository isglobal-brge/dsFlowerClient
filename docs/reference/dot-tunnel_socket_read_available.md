# Read currently available bytes without hiding a closed/broken socket

Read currently available bytes without hiding a closed/broken socket

## Usage

``` r
.tunnel_socket_read_available(socket, max_bytes)
```
