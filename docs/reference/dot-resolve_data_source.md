# Resolve a data source deterministically

Checks if the data string is a resource name or an existing symbol. No
heuristics – checks the actual server state.

## Usage

``` r
.resolve_data_source(data, conns)
```
