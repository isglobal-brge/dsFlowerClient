# Refuse to overwrite session symbols during transactional initialization

Refuse to overwrite session symbols during transactional initialization

## Usage

``` r
.dsi_require_symbols_absent(conns, symbols)
```
