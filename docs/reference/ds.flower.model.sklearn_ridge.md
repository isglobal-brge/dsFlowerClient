# Create a scikit-learn Ridge Classifier model spec

Create a scikit-learn Ridge Classifier model spec

## Usage

``` r
ds.flower.model.sklearn_ridge(alpha = 1)
```

## Arguments

- alpha:

  Numeric; regularization strength.

## Value

A `dsflower_model` S3 object.
