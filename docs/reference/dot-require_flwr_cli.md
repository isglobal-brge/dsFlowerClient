# Ensure the Flower CLI is available

Checks the venv first. If not healthy, attempts to create it on-the-fly.

## Usage

``` r
.require_flwr_cli()
```

## Value

Invisible TRUE, or stops with an error.
