# Build the exact typed dsFlower boosting-style parameter array for request v1

Build the exact typed dsFlower boosting-style parameter array for
request v1

## Usage

``` r
.native_tree_boosting_parameter_values(params, engine)
```
