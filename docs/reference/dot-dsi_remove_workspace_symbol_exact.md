# Remove a plain workspace symbol and verify the per-node postcondition

Remove a plain workspace symbol and verify the per-node postcondition

## Usage

``` r
.dsi_remove_workspace_symbol_exact(conns, symbol)
```
