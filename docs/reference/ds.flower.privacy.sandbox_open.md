# Create a sandbox_open privacy spec

Minimal restrictions. Requires explicit admin opt-in on the server
(`dsflower.allow_sandbox = TRUE`).

## Usage

``` r
ds.flower.privacy.sandbox_open()
```

## Value

A `dsflower_privacy` S3 object with mode = "sandbox_open".
