# Stop a Flower run

Invokes `flwr stop` for a specific run.

## Usage

``` r
ds.flower.run.stop(run_id)
```

## Arguments

- run_id:

  Character; the run ID.

## Value

Character; output of flwr stop.
