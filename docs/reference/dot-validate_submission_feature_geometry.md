# Validate architecture geometry against the public feature schema

Validate architecture geometry against the public feature schema

## Usage

``` r
.validate_submission_feature_geometry(model_name, sub, features)
```
