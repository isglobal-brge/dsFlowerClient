# Compare metrics across multiple training runs

Compare metrics across multiple training runs

## Usage

``` r
ds.flower.compare(...)
```

## Arguments

- ...:

  Named `dsflower_result` objects to compare.

## Value

A data.frame with columns: run, round, metric, value.
