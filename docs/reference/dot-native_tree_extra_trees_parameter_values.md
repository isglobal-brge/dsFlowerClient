# Build the exact typed ExtraTrees parameter array for request v1

Build the exact typed ExtraTrees parameter array for request v1

## Usage

``` r
.native_tree_extra_trees_parameter_values(params)
```
