# Core survival profile, matching the executed campaign runtime.
# Install the CPU torch wheel first; platform-specific campaign extras are omitted.
torch==2.14.0
numpy==2.4.6
pandas==3.0.6
pyarrow==25.0.1
flwr==1.31.0
cryptography==46.0.7
opacus==1.6.0
scipy==1.17.1
